<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <title>Main Page</title>
    </head>
    <body>
        <div class="container text-center">
            <h1>HairSQL Barber Shop</h1>
        </div><hr>
        <div class="jumbotron row text-center">
            <div class="col-md-6">
                <p>Create a new Stylist here:</p><hr>
                <div class="form-group">
                    <form action='/stylists' method='post' class="form-inline">
                        <label for='new_stylist'>New Stylist Name</label>
                        <input id='new_stylist' name='new_stylist' type='text' class="form-control"><br><br>
                        <button type='submit' class="btn btn-success">Add Stylist</button>
                    </form>
                </div>
            </div>
            <div class="col-md-6">
                {% if stylists is not empty %}
                    <p>All Existing Stylists:</p>
                    <ul>
                    {% for stylist in stylists %}
                        <li>{{ stylist.getStylistName }}</li>
                    {% endfor %}
                    </ul>
                {% endif %}
                <form action='/delete_stylists' method='post'>
                    <button type='submit' class="btn btn-danger">Delete All</button>
                </form>
            </div>
        </div>
        <a href="/" role="button" class="btn btn-lg btn-info">Back to Main Page</a>

    </body>
</html>
