<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <title>Clients</title>
    </head>
    <body>
        <div class="container text-center">
            <h1>HairSQL Barber Shop</h1>
        </div><hr>
        <div class="jumbotron row">
            <div class="col-md-6 text-left">
                <p>Create a New Client Here:</p><hr>
                <div class="form-group">
                    <form action='/clients' method='post' class="form-inline">
                        <label for='new_client'>New Client Name</label>
                        <input id='new_client' name='new_client' type='text' class="form-control" required><br><br>
                        <button type='submit' class="btn btn-success">Add Client</button>
                    </form>
                </div>
            </div>
            <div class="col-md-6 text-left">
                <p>All Existing Clients:</p>
                {% for client in clients %}
                    <li>{{ client.getClientName }}</li>
                {% endfor %}
        
                <form action='/delete_clients' method='post'>
                    <button type='submit' class="btn btn-danger">Delete All</button>
                </form>
            </div>
        </div>
        <a href="/" role="button" class="btn btn-info">Back to Main Page</a>

    </body>
</html>
